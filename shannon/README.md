# Shannon

A code analysis and testing framework.

## Quick Start

```bash
pnpm install
pnpm run build
./shannon start -u <url> -r /path/to/repo
```

## Project Structure

- `apps/cli/` — CLI entry point
- `apps/worker/` — Worker process
